Given the following WSDL: https://www.w3schools.com/xml/tempconvert.asmx?WSDL

Create a new SOAP Project in SoapUI by providing a name (TempConvert) and the WSDL URL, leave everything else on the default settings for now.

Inspect the generated project:

How many endpoints does the web service have?
	2
    
What are the names of the endpoints?
	CelsiusToFahrenheit
	FahrenheitToCelsius
	
What are the input parameters of the endpoints?
	egy darab szám ami lehet egész vagy törtszám is
